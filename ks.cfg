#Generated by Kickstart Configurator
#platform=x86

#System language
lang en_US
#Language modules to install
langsupport en_US
#System keyboard
keyboard us
#System mouse
mouse
#System timezone
timezone Europe/Stockholm
#Root password
rootpw --iscrypted $1$UZmAXveI$LxzY3VYNaj7NFCPAuNpOf.
#Initial user
user stack --fullname "stack" --iscrypted --password $1$33hepibP$HRTy2TVnNou7Qt72ayekj.
#Reboot after installation
reboot
#Use text mode install
text
#Install OS instead of upgrade
install
#Use CDROM installation media
cdrom
#System bootloader configuration
bootloader --location=mbr 
#Clear the Master Boot Record
zerombr yes
#Partition clearing information
clearpart --all --initlabel 
#Disk partitioning information
part /boot --fstype ext2 --size 1030 
part swap --size 32000 
part / --fstype xfs --size 1 --grow 
#System authorization infomation
auth  --useshadow  --enablemd5 
#Firewall configuration
firewall --disabled 
#Do not configure the X Window System
skipx
%post --interpreter=/bin/bash
echo "#!/bin/sh
#readonly TMOUT=900
readonly HISTFILE
 
# Set Bach history to large size and better time format
export HISTTIMEFORMAT='%F %T #'"
export HISTSIZE=50000
shopt -s histappend histverify
 
# write previous command to history file before display the prompt
# Might cause issues for some commands
PROMPT_COMMAND='history -a'
# HISTCONTROL=ignoreboth
# HISTCONTROL=ignoredups " > /etc/profile.d/os-tune.sh

cp /etc/network/interfaces /etc/network/interfaces.bkp

echo "# This file describes the network interfaces available on your system
# and how to activate them. For more information, see interfaces(5).
# The loopback network interface
auto lo
iface lo inet loopback
# The primary network interface
auto em2.181
iface em2.181 inet static
        address 10.210.4.130
        netmask 255.255.255.240
        network 10.210.4.128
        broadcast 10.210.4.143
        gateway 10.210.4.129
        # dns-* options are implemented by the resolvconf package, if installed
        dns-nameservers 150.132.95.40 150.132.95.20
        dns-search seli.gic.ericsson.se
auto pxeboot0
iface pxeboot0 inet static
    address 192.168.0.19
        netmask 255.255.255.0
        bridge_ports em1
        bridge_stp off
        bridge_fd 0
        bridge_maxwait 0
auto br-fw-admin
iface br-fw-admin inet manual
        bridge_ports em2
        bridge_stp off
        bridge_fd 0
        bridge_maxwait 0 " > /etc/network/interfaces
